# TO DOs

- [ ] Make the platform.yaml file default to a relative file and path so the config does not have to be specified.
- [ ] Add more examples to the README.md file.
- [ ] Add some type of validation to ensure that the output paths specified in platform.yaml are valid and match the expected output paths in the OutputPaths model.
- [ ] Add some type of validation to make sure the generated files are valid YAML files.
- [ ] At the completion of the generation process, output a summary of what was generated, similar to how kubectl outputs a summary after applying manifests.
- [ ] Add support for creating a common git branch for all the files. Then commit to that branch and push to the remote.
- [ ] Add more tests.
- [ ] Add some reasonable defaults to the platform.yaml file so a minimal config can be used.
- [ ] Add a Kong API Gateway Generator
- [ ] Add an External Secret Generator
- [ ] Add a Certificate Generator
